# ✝️ CV PIONEERING TESTIMONIES DESIGNER (V4 Fixed Logo)

Fixed logo path handling and dynamic image inversion for dark UI!

## 🚀 How to Run Locally

1. Extract this zip file into a folder.
2. Open terminal/cmd in that folder.
3. Run the following commands:
   ```bash
   pip install -r requirements.txt
   python -m streamlit run app.py
   ```
